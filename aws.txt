asg-7 host a website on ec2:

sudo apt-get update
sudo apt-get upgrade
sudo apt install nginx
cd ../../var
ls
cd www
ls
sudo cdmod 777 html

/var/www/html

asg-8 local machine to GitHub file push:

git init
git remote add origin GitHub link
git add .
git commit -m "first commit"
git branch -M master
git push -u origin master

git clone

asg-9 
sudo apt-get update
sudo apt-get upgrade
sudo apt install nginx
clone githublink
cd aws_asg
curl -sL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install nodejs
npm install
node index.js
git pull

